---
description: >
  A human-facing explanation of how this project's code is organised — its parts, their responsibilities,
  and how they fit together. Written for a person reading it away from the codebase: a new engineer, a
  reviewer, a stakeholder. Favours diagrams and worked examples over exhaustive detail, and may go stale
  between releases without harming anything, because nothing depends on it.
  Produced ON REQUEST. Agents do not maintain this file and do not read it to do the work — the
  authoritative version is `kia-context/specs/ARCHITECTURE.md`.
authority: artifact
writes: agent, on request
status: empty
covers: "{{what it describes}}"
last_updated: "{{YYYY-MM-DD}}"
---

# Software Architecture — {{Project}}

> **The layout below is a sample, not a requirement.** Keep it, drop it, reorder it, or replace it
> entirely with whatever this project actually needs — you are not filling in a form, and an empty
> section is worse than a missing one. The one thing that must **not** change is the frontmatter above:
> those six fields are what make this file part of kiacontext.

**Contents** · [At a glance](#at-a-glance) · [The parts, and what each owns](#the-parts-and-what-each-owns) · [How a change flows through them](#how-a-change-flows-through-them) · [The decisions worth knowing about](#the-decisions-worth-knowing-about)

> Not written yet. Delete this file if this project does not need one.

## At a glance

```mermaid
graph LR
  A[{{part}}] --> B[{{part}}]
```

## The parts, and what each owns

## How a change flows through them

## The decisions worth knowing about

> Point at `kia-context/logs/BRAINSTORM.md` for the full reasoning rather than repeating it here.
